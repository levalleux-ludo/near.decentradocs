// @nearfile

import { PersistentVector } from "near-sdk-as";

// --- contract code goes below

export function docExists(docId: string): bool {
  return false;
}

export function registerDoc(
  docId: string,
  encryptedKey: string,
  subscriptionFee: u64,
  authorizedAddresses: string[] ): void {
}

export function getAuthorizedAccounts(docId: string): string[] {
  return [];
}

